# Lightweight correctness checks; no application architecture or SDK-floor changes.
analyzer:
  exclude:
    - build/**
    - '**/*.g.dart'
linter:
  rules:
    - cancel_subscriptions
    - close_sinks
    - collection_methods_unrelated_type
    - no_duplicate_case_values
    - throw_in_finally
    - valid_regexps
